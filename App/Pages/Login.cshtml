@page "/"
@model App.Pages.LoginModel
@{
    ViewData["Title"] = "Login";
}

<h2>Login</h2>


<form method="post" asp-page-handler="Login">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    
    <div class="form-group">
        <label asp-for="Login.email">Email</label>
        <input asp-for="Login.email" class="form-control" style="max-width: 15%;" />
        <span asp-validation-for="Login.email" class="text-danger"></span>
    </div>
    
    <div class="form-group">
        <label asp-for="Login.password">Password</label>
        <input asp-for="Login.password" type="password" class="form-control" style="max-width: 15%;" />
        <span asp-validation-for="Login.password" class="text-danger"></span>
    </div>
    
    <button type="submit" class="btn btn-primary mt-2">Login</button>
</form>
<br>
<br>
<div>

<form method="post" asp-page-handler="Register">
        <span>Have you an account? If you not </span>
    <button type="submit" class="btn btn-link ">register here</button>
</form>
</div>

@* @if (!string.IsNullOrEmpty(Model.Token))
{
    <div class="alert alert-success mt-3">
        <p><strong>Login Successful!</strong></p>
        <p><strong>You will move to user page.</strong> </p>
        <a href="/UserPage" class="btn btn-success">Go to UserPage</a>
    </div>
} *@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}